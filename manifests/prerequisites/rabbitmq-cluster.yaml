apiVersion: rabbitmq.com/v1beta1
kind: RabbitmqCluster
metadata:
  name: rabbitmq
spec:
  replicas: 1
  # Defaults to 1 cpu, impossible for the GitHub runner
  resources:
    requests:
      cpu: 50m
      memory: 1Gi
    limits:
      cpu: 1000m
      memory: 2Gi
